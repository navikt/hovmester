# Aksel (@navikt/ds-*) er publisert på public npm. Pinn scopet hit slik at
# CI (uten GitHub Packages-token) kan installere uten 401. Overstyrer en
# eventuell global @navikt -> npm.pkg.github.com-konfig.
@navikt:registry=https://registry.npmjs.org/
